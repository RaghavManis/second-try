# The frontend API base URL should map to the live Render backend URL.
# Do not hardcode this value if deploying via Vercel - instead, set this variable directly in the Vercel Dashboard!
VITE_API_BASE_URL=https://your-backend-url.onrender.com/api
